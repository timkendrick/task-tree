---
title: "Add `--checkout` flag to `tt task create` command"
status: TODO
description: "If the user provides the `--checkout` argument to `tt task create`, run the checkout command on the newly-created task before exiting the command\n\nAlso accepts an `--worktree` argument if `--checkout` is specified."
---
